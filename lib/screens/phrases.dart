
import 'package:flutter/material.dart';

class Phrases_Screen extends StatelessWidget {
  const Phrases_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
