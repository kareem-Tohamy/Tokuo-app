
import 'package:flutter/material.dart';

class Family_Members_Screen extends StatelessWidget {
  const Family_Members_Screen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
